# In Ruby, we have the Dog object(definition) and we have instances of the Dog object
# There are class methods(calling a method on a definition) and instance methods,
# Class Method ex: Dog.random
# .self in Ruby is .this in Javascript --> needs to be inside the class, otherwise .self is just
# referring to main
#
