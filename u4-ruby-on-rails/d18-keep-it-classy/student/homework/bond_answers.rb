require_relative 'bond_data.rb'

include Bond

movie = Bond::MOVIE_DATA#[1][:name]

sum = 0
def get_gross_for(movie_title, movie_data)
  movie_data.each do |movie_title|
    movie.gross.split(,).join
    sum += movie.gross
    sum
end
